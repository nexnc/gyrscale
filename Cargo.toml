[package]
name = "gyrscale"
version = "0.1.0"
edition = "2024"

[dependencies]
libc = "0.2.189"

[lints.clippy]
pedantic = "deny"

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
panic = "abort"
strip = true
